{
  "name": "@oem/ml-pricing-extension",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "vite build && vite build --config vite.content.config.ts && node scripts/validate-build.mjs",
    "package": "pnpm build && mkdir -p release && rm -f release/oem-ml-pricing-extension-0.1.0.zip && cd dist && zip -qr ../release/oem-ml-pricing-extension-0.1.0.zip . -x '.DS_Store' '*/.DS_Store'",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@oem/contracts": "workspace:*",
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.326",
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0",
    "@vitejs/plugin-react": "6.0.5",
    "jsdom": "30.0.1",
    "typescript": "^5.7.3",
    "vite": "8.1.3",
    "vitest": "4.1.9"
  }
}
